// Array.from(document.querySelectorAll('.piece'))
// .map(piece => piece.addEventListener('ondragstart', drag));

// Array.from(document.querySelectorAll('.tile'))
// .map(tile => {
//     tile.addEventListener('ondrop', drop);
//     tile.addEventListener('ondragover', allowDrop);
// });